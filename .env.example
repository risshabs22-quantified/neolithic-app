# Optional: use a local SQLite file for development (default if unset: app userData neolithic.db)
# DATABASE_URL="file:./prisma/dev.db"

# Optional if unset: auto-generated once and saved to userData/neolithic-secrets.json
# BETTER_AUTH_SECRET=
# STORE_ENCRYPTION_KEY=
